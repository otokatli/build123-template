# A Template Project for Building 3D Parts using build123d

The project template for CAD using build123d.

Install the project requirements can be installed using

```
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

For visualization, run the following code inside the virtual environment.

```
python -m ocp_vscode
```
